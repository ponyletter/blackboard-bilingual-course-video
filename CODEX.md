# Codex Adapter

Read `AGENTS.md` as the authoritative workflow, then read `skill/SKILL.md`. Keep all generated course data and media inside a dedicated topic directory.

Use shell-accessible FFmpeg and the bundled Python scripts for deterministic subtitle, manifest, packaging, and validation work. If a browser, TTS service, image model, or HTML-to-video renderer is not configured, stop before final production and state the required integration. Do not replace the visual contract with arbitrary slides or generic stock footage.

For each bilingual request, keep Chinese and English render projects separate. Use the same reasoning structure while generating independent audio, measured timings, captions, question-led covers, social posts, scene images, manifests, and archives.
