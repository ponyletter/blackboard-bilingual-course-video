# Claude Adapter

Read `AGENTS.md` first and treat it as the primary production contract. Then read `skill/SKILL.md` and the required references named there.

Before generating any final asset, report whether this environment has: web research, a configured TTS provider, image generation, a renderer that can export 1920×1080 MP4, FFmpeg, and Git credentials. Use configured equivalents where possible; do not claim that the repository itself provides media APIs or credentials.

For a new course, require a filled `templates/course_request.md`, create separate Chinese and English scene data, render each language independently, and run the bundled validation script before delivery.
